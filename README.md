# PALM

## Usage

```bash
# install elixir in archlinux
$ sudo pacman -S elixir inotify-tools
$ cd app_name
$ mix deps.get
$ cd assets && npm install && cd ..
$ mix help
# format all HEEX templates
$ mix format
# examining routes
$ mix phx.routes
$ mix phx.server
```
